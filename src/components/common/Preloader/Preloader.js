import preloader from "../../../assets/images/loading.gif";
import React from "react";

const Preloader = () => {
    return <img src={preloader} width={150} height={100} />
}

export default Preloader;
